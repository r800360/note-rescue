# Local app state
data/state.json
data/dismissed_todos.json

# Local generated data
data/raw_backups/
data/indexes/
data/reports/
data/logs/

# Private notes
vault/

# Private scholar / mentor data (NEVER commit to a public repo)
config/scholars.json
config/scholars.local.json
data/scholars/

# Local settings overrides (optional)
config/settings.local.json

# Private API keys and chat memory (NEVER commit)
config/secrets.local.json
.env
data/chat_corrections.json
data/chat_sessions/

# Personal site — drafts and built output stay local until you review
config/site.profile.public.json
site/drafts/
site/sources/
site/public/
site/dist/

# Python
.venv/
__pycache__/
*.pyc
